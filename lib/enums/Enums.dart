enum ActivityValue { activity1, activity2, activity3, activity4 }

enum DataChangeMode {
  onDeviceAdded,
  onDeviceUpdated,
  onDeviceRemoved,
  onDeviceChanged
}

enum DeviceOption { option1, option2, option3, option4, option5 }
